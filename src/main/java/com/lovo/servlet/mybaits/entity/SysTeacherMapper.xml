<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lovo.servlet.mybaits.dao.ISysTeacherDao">

    <insert id="savaTeacher" parameterType="sysTeacher">
        insert into sys_teacher(t_id, t_name,t_class)
        values(#{tId},#{tName},#{tClass})
    </insert>


</mapper>